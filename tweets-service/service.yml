type: worker
description: Generic MongoDB storage service
author: originate

dependency-data:
  exocom:
    receives:
      - create tweet
      - list tweets
      - get tweet details
      - update tweet
      - delete tweet
    sends:
      - tweet created
      - tweets listed
      - tweet details
      - tweet not found
      - tweet updated
      - tweet deleted
      - tweet not created

development:
  scripts:
    run: node src/server.js
    test: /node_modules/.bin/cucumber-js

remote:
  cpu: 100
  memory: 500
  secrets:
    - MONGODB_USER
    - MONGODB_PW
